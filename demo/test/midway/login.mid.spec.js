'use strict';

/*describe('Midway: Testing Controllers', function () {

    var tester;
    beforeEach(function () {
        if (tester) {
            tester.destroy();
        }
        tester = ngMidwayTester('App.login');
    });

    it('should load the VideosCtrl controller properly when /videos route is accessed', function (callback) {
        tester.visit('/videos', function () {
            tester.path().toEqual('/videos');
            var current = tester.inject('$route').current;
            //var controller = current.controller;
            //var scope = current.scope;
            expect(controller).toEqual('VideosCtrl');
            callback();
        });
    });

});*/

/*describe('Midway: Login controller', function () {

    beforeEach(function () {
        browser().navigateTo('/');
    });

    it('should have a working login page controller', function () {
        browser().navigateTo('#/login');
        expect(browser().location().path()).toBe('/login');
        expect(element('[ui-view]').html()).toContain('Welcome to Internet Banking');
    });

});*/
